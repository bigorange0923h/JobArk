# 仅作为本地开发示例；复制为 .env 后必须替换密码，绝不提交 .env。
# 本文件仅供仓库根目录的 compose.yaml 使用；后端应用配置见 backend/.env.example（JOBARK_ 前缀）。
POSTGRES_DB=jobark
POSTGRES_USER=jobark_app
POSTGRES_PASSWORD=请替换为本地开发密码
POSTGRES_PORT=5432
