# JobArk 项目规范

本规范适用于仓库内所有人工与自动化改动。与现有架构文档冲突时，以安全性、数据真实性和用户显式确认优先。

## 1. 开发原则

- 先核对当前代码、迁移和测试状态，再修改；不要把目录骨架、设计文档或 AI 输出当作已实现事实。
- 按业务领域组织后端：`profile`、`resume`、`job`、`matching`、`application`、`dashboard`。禁止无归属地向全局 `services/`、`utils/` 堆放业务逻辑。
- `Profile` 是事实和证据库，`Resume` 是其可版本化表达；AI 不能虚构技能、经历、学历、项目或成果。
- 业务事实由数据库保存。LLM、浏览器会话、缓存和前端状态均不能作为唯一事实来源。
- 一切外部副作用（尤其后续的投递、打招呼或表单提交）必须有显式确认、审计记录和可理解的失败反馈。

## 2. 需求、计划与执行

- `docs/requirements/` 保存已确认的产品范围、非目标和验收标准；需求变更必须先更新对应文档，再实施代码改动。
- `docs/architecture.md` 与 `docs/adr/` 保存稳定的架构边界和技术决策；不要把临时任务状态写入这些文件。
- GitHub Issues/Project 是协作任务状态的唯一来源。每个可执行工作项应关联需求、验收条件、优先级、依赖和状态（Todo/In Progress/Blocked/Done）。
- `docs/local/` 仅允许本地推演和草稿，不能作为协作承诺、发布依据或代码运行时依赖。
- 实施前确认 Issue 的验收条件和依赖；实施后附上测试/构建证据并更新 Issue 状态。不要同时维护 GitHub 状态和仓库 `TODO.md` 两份任务真相。

## 3. 注释与文档

- 每个 HTTP 接口必须写明用途、输入/输出语义、权限或确认要求，以及可能的主要错误；FastAPI 路由应提供 `summary`、`description` 和响应模型。
- 每个公共函数、类、方法、异步任务和跨模块接口必须使用 docstring 说明职责、参数、返回值、异常/副作用。私有函数在意图不能从名称和类型直接看出时也必须注释。
- 注释一律使用中文，解释“为什么”和约束，不重复代码字面含义；复杂状态流转、评分规则、去重规则和外部平台适配必须记录假设与边界。类名、函数名、API 路径、错误码和第三方技术术语仍按既有英文命名规范书写。
- Pydantic 请求/响应模型、枚举和数据库迁移需有清晰名称和字段说明；对外 API 的变更必须同步更新 OpenAPI 描述和相关文档。

## 4. API 响应与异常处理

所有 JSON API 成功响应统一为：

```json
{
  "success": true,
  "data": {},
  "meta": { "request_id": "..." }
}
```

列表或分页结果放在 `data`，其分页信息放在 `meta`。错误响应统一为：

```json
{
  "success": false,
  "error": {
    "code": "RESOURCE_NOT_FOUND",
    "message": "面向用户的安全提示",
    "details": []
  },
  "meta": { "request_id": "..." }
}
```

- 使用语义正确的 HTTP 状态码；成功不以 HTTP 200 包装失败。验证失败为 422，未认证为 401，无权限为 403，资源不存在为 404，冲突为 409，限流为 429，未预期错误为 500。
- 领域异常必须在统一异常处理器中映射为稳定错误码。不得在路由中散落 `try/except Exception`，不得吞掉异常或返回裸字符串。
- 记录结构化日志和 `request_id`；服务端日志可保留诊断信息，但响应不得暴露堆栈、密钥、连接串、Cookie、令牌或上游原始错误。
- 调用 LLM、浏览器、数据库或外部 HTTP 服务时，必须设置超时、明确可重试范围和错误映射。写操作不得因不确定重试而重复提交。

## 5. 数据、AI 与自动化

- 数据模型、迁移、服务层和 API 必须有明确所有者；数据库结构一律通过 Alembic 迁移变更，不手工改生产库。
- `Application` 状态只能通过 `ApplicationEvent` 变更；投递时必须绑定不可变的 `ResumeVersion` 与对应 JobSnapshot。
- 匹配输出必须保存版本化输入、证据引用、缺口与不确定项；分数不能脱离证据单独作为决策依据。
- AI 产生的简历、问候语或答案先作为候选草稿。只有用户确认的内容可以进入提交类流程。
- 浏览器自动化必须位于适配器层；平台页面改动、验证码、登录失效或服务条款限制只能令任务失败或等待人工，不能污染核心领域数据。

## 6. 质量与安全

- Python 使用类型标注，遵循 Ruff、Pyright 和 Pytest；前端使用 TypeScript 严格检查、lint 与生产构建。每次改动运行与风险相称的检查并如实报告结果。
- 新增功能至少覆盖正常路径、输入校验、未找到、权限/确认、冲突和外部依赖失败路径；修复缺陷时先增加可复现测试。
- 配置通过环境变量和 `.env.example` 管理；严禁提交 `.env`、密钥、真实个人数据、会话 Cookie、浏览器 Profile 或生产导出数据。
- 依赖要最小化并锁定；新增第三方服务、浏览器自动化或外部平台集成前，记录用途、数据流向、条款风险和降级方案。
- 不做未验证的性能、安全或合规宣称。迁移、删除、批量更新、外部提交和生产部署必须先确认目标与影响范围。

## 7. Git 与提交

- 每个改动保持聚焦；不要混入 IDE 文件、生成文件、临时数据或无关格式化。
- Git 提交信息一律使用中文，简明描述改动目的；推荐使用“模块：动作/结果”的格式，例如 `简历：新增版本创建接口`。
- 提交前检查 `git status` 与 diff，确认未包含机密信息或无关文件。
- 文档与实现必须同步：改变目录、API 契约、数据模型、流程或 V1 范围时，更新 `docs/architecture.md` 和相关测试。
