# 后端应用配置示例：复制为 backend/.env 后按需修改；.env 已被 Git 忽略，绝不提交。
# 变量统一使用 JOBARK_ 前缀。本文件不含数据库配置：PostgreSQL 的 POSTGRES_* 由仓库根目录
# .env 提供给 compose.yaml，后端不读取它。
# 所有配置项都有默认值，因此缺少 backend/.env 时服务仍可直接启动。

# 运行环境标识：local | test | prod
JOBARK_APP_ENV=local

# 日志级别：DEBUG | INFO | WARNING | ERROR | CRITICAL
JOBARK_LOG_LEVEL=INFO

# 业务领域路由的统一挂载前缀；/health 等运维接口不受其影响。
JOBARK_API_V1_PREFIX=/api/v1

# 异步数据库连接串，驱动固定为 asyncpg。密码必须与仓库根目录 .env 的 POSTGRES_PASSWORD 一致。
# 默认值是本地占位串：未配置时服务仍能启动，但首次查询会以认证失败暴露问题。
JOBARK_DATABASE_URL=postgresql+asyncpg://jobark_app:请替换为本地开发密码@127.0.0.1:5432/jobark

# 是否把 SQL 语句与参数写入日志；仅本地排查使用，默认关闭。
JOBARK_DATABASE_ECHO=false
